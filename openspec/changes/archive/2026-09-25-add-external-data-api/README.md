# add-external-data-api

Split API-key access into a portal-read scope (anonymized, published-only, mirrors /portal/*) and an own-data scope (raw, ownership-checked, paginated up to 1000/page) exposed under /external/*
